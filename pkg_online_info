#!/bin/sh

set -e

. pkg_online_server.sh

export LC_ALL=C 

#
usage (){
    echo 'usage: pkg_online_info <bin|src> [packages...]' 1>&2
}

if test $# -lt 2; then
    usage
    exit 1
fi

#
binsrc=$1
shift

#
dict -h "$PKG_ONLINE_SERVER" -p "$PKG_ONLINE_PORT" -d pkgsrc-"$binsrc"-info "$@"
